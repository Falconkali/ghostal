import { CustomCheckoutPreview } from "@/components/checkout-preview";

export const metadata = {
  title: "Custom Checkout Preview — Ghostal",
  description: "Experience Ghostal's custom branded checkout experience.",
};

export default function CheckoutPreviewPage() {
  return <CustomCheckoutPreview />;
}
